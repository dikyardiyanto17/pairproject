<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <title>Mau ganti?</title>
</head>
<body>
    <%- include('partial/header2') %>
    <div style="text-align: center; background-color: blue; opacity: 0.8; padding: 20px; width: 700px; margin: 10px auto; color: beige; border-radius: 10px;">
        <h1>Diganti apa?</h1>
    <form action="/profile/<%= profile.id %>/edit" method="post">
        <p>
            Nama panggilannya apa lagi?<br>
            <input type="text" name="fullname" value="<%= profile.fullname %>">
        </p>
        <p>
            Mau ganti photo? <br>
            <input type="text" name="photo" value="<%= profile.photo %>">
        </p>
        <p>
            Bionya diubah apa lagi? <br>
            <textarea name="bio"cols="30" rows="10"><%= profile.bio %></textarea>
        </p>
        <p>
            Ganti nomor gara-gara ditagih debt collector? <br>
            <input type="text" name="phone" value="<%= profile.phone %>">
        </p>
        <p>
            Ganti jenis kelamin? <br>

            <% gender.forEach(x => { %>
                <% if (profile.gender == x){ %>
                <input type="radio" name="gender" value="<%= x %>" checked="checked"> <%= x %>
                <% } else { %>
                <input type="radio" name="gender" value="<%= x %>"> <%= x %>
                <% } %>
            <% }) %>
        </p>
        <input type="submit">
    </form>
    </div>
</body>
</html>